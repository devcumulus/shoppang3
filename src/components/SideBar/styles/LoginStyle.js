import styled from "@emotion/styled";

export const Login = styled.div`
  position: relative;
  width: 220px;
  height: auto;
  padding: 16px;
  img {
    position: relative;
    margin-top: 12px;
  }
  p {
    position: relative;
    font-size: 20px;
    margin-top: 24px;
    cursor: pointer;
  }
  span {
    position: relative;
    color: #666;
    line-height: 24px;
    cursor: pointer;
  }
`;
